% maven

# Initialize build state, e.g. set properties or create directories

mvn initialize


# Remove all files generated by the previous build

mvn clean


# Run tests using a suitable unit testing framework

mvn test


# Run tests, recompile all code and package it in its distributable format, such as a JAR

mvn clean package


# Check for updates to dependencies in project

mvn versions:display-plugin-updates


# See the dependency table

mvn dependency:tree 
