#                                __ _                   
#                         ___ _ / /(_)___ _ ___ ___  ___
#                        / _ `// // // _ `/(_-</ -_)(_-<
#                        \_,_//_//_/ \_,_//___/\__//___/
#                                                       
#

alias code='/usr/share/vscodium-bin/codium'
alias mpd='mpd ~/.mpd/mpd.conf'
alias yolo='git commit -m "$(curl -s http://whatthecommit.com/index.txt)"'
alias :wq="echo You\'re not in vim dumbass!"

# so that alias can be sudo'ed
alias sudo='sudo '

# ips
alias ip_publ="dig +time=1 +tries=1 +short myip.opendns.com @resolver1.opendns.com"
alias ip_priv="ip route get 1 | awk -F'src' '{print $2; exit}' | awk -F' uid' '{print $1;exit}' | rg -o 'c.*u' | cut -d ' ' -f 2"
alias ggraph="git log --oneline --decorate --graph"
alias swipl.="swipl"
alias nps="npm "
